import { ProjectDetailView } from './ProjectDetailView';
import { MA_PROJECT_DOMAIN } from './projectDomain';

/** M&A 관리 상세. */
export function MaProjectDetailView() {
  return <ProjectDetailView domain={MA_PROJECT_DOMAIN} />;
}
