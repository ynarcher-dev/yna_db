-- =============================================================================
-- 0007_partners_department.sql — 협력사 부서명 컬럼 추가
-- 기업/기관명에 더해 담당 부서(예: 창업진흥과, 오픈이노베이션팀)를 기록한다.
-- 선행: 0001_schema.sql / 재실행 안전(idempotent).
-- =============================================================================
ALTER TABLE public.partners ADD COLUMN IF NOT EXISTS department VARCHAR(100);
